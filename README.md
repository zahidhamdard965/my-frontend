# React Node.js MySQL CRUD Tutorial

Full-stack application with React frontend, Node.js/Express backend, and MySQL database.

## Project Structure
react-node-express-mysql-crud/
├── backend/ # Node.js Express API
└── frontend/ # React frontend application

text

## Setup Instructions

### Backend Setup
```bash
cd backend
npm install
node server.js
Frontend Setup
bash
cd frontend
npm install
npm run dev
Access URLs
Backend API: http://localhost:5000

Frontend App: http://localhost:5173

Features
Create, Read, Update, Delete (CRUD) tutorials

MySQL database integration

React Router for navigation

Tailwind CSS for styling

RESTful API with Express
